U SPORTS Presto Live V36

Changes:
- Embedded McMaster and Guelph logos from the official OUA live-stat page so these two do not depend on third-party hotlinking.
- Removed all field-goal posts from GameCast.
- Re-armed the verified OUA/Presto poller while the pregame screen is visible and on direct #live deep links.
- Added a small live-feed status line in pregame so it is obvious whether the watcher is active.
- Keeps 10-second polling and uses only the public Presto live-update request; no cookies, credentials, or access-control bypass.

Deploy all files at repository root.
