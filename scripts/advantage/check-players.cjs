const assert=require('assert'),fs=require('fs'),path=require('path'),vm=require('vm');const root=path.resolve(__dirname,'../..');global.AdvantageModel=require(path.join(root,'advantage-model.js'));require(path.join(root,'player-leaders.js'));const P=global.US_PlayerLeaders,d=JSON.parse(fs.readFileSync(path.join(root,'data/player-leaders-usports.json')));
assert(d.games.every(g=>g.date.startsWith('2026-')&&g.date<d.asOf));
const fixture={season:2026,asOf:'2026-09-23',games:[{date:'2026-09-01',source:'https://example.com/a',teams:{Alberta:{passing:[{name:'A',yards:200}]}}},{date:'2026-09-15',source:'https://example.com/b',teams:{Alberta:{passing:[{name:'B',yards:100}]}}},{date:'2025-09-01',teams:{Alberta:{passing:[{name:'B',yards:9999}]}}},{date:'2026-09-25',teams:{Alberta:{passing:[{name:'B',yards:9999}]}}}]};
const g={date:'2026-09-25',away:'alberta',home:'acadia'},p=P.project(fixture,g);assert.equal(p[0].teams[0].leader.name,'B');assert.equal(p[0].teams[0].leader.yards,50);assert.equal(p[0].teams[1].leader,null);assert.deepEqual(P.project({...fixture,season:2025},g),[]);assert.deepEqual(P.project(fixture,{...g,date:'2026-09-01'}),[]);
const real=P.project(d,{...g,home:'calgary'});assert(real.every(r=>r.teams.every(t=>t.leader&&Number.isFinite(t.leader.yards))));assert(!P.card(d,{...g,home:'calgary'}).includes('NaN'));console.log('Player season, kickoff cutoff, missing rows, latest candidates and real matchup checks passed.');
